# Keyframes-Extractor
A Python tool designed to extract unique static keyframes from videos, focusing on stable scenes rather than transitions. Ideal for analyzing screen recordings, such as iOS app demonstrations, by capturing representative frames of distinct application states. Built with OpenCV and NumPy.
